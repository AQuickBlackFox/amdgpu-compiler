#pragma once

typedef enum {
    Bool,
    Char,
    Short,
    Half,
    Int,
    Float,
    Long,
    Double,
} DataType;

typedef enum {
    UnSigned,
    Signed
} PreDataType;

typedef enum {
    Device,
} Qualifier;

typedef enum {
    Buffer,
} Attribute;

